import ReservationClientPage from "./client";

export default function ReservationPage() {
  return (
    <section className="container">
      <ReservationClientPage />
    </section>
  );
}

export const metadata = {
  title: "Reservation",
};
